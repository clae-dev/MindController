# 마음 들여다보기 · MindController — 학과 학술제 발표 가이드

> **AI응용소프트웨어공학과 학과 학술제** · 2026.06.27(토) 14:30 ~ · HIT(701관) 2층 대강의실
> 라이브: https://mindcontroller-theta.vercel.app

---

## 핵심 전략: "데모 먼저, 설명은 그 위에"

이 앱은 웹캠 앞에 서면 바로 결과가 나오는 **인터랙티브 서비스**입니다.
보통의 "배경 → 기술 → 데모" 순서를 **거꾸로** 가야 먹힙니다.
청중이 결과를 먼저 보고 "어떻게 한 거지?"가 생긴 다음에 기술을 풀어야 집중이 유지됩니다.

---

## 발표 구성 (약 8~10분 기준)

### ① 훅 — 30초
- 슬라이드 없이 바로 시작: *"지금 제 얼굴만 보고 스트레스 지수랑 심박수를 맞춰볼게요. 손도 안 댑니다."*
- 곧장 라이브 실행

### ② 라이브 데모 1: 마음 분석 — 2분
- 본인 얼굴로 10초 측정 → 결과 화면(스트레스 지수, 감정 분포, 심박, 오늘의 한마디)
- 핵심 한 마디: **"영상은 서버로 안 갑니다. 전부 브라우저 안에서 끝나요."**
  → 프라이버시가 가장 큰 차별점

### ③ 라이브 데모 2: 긴장 감지 — 2분
- 청중 자원자 1명 참여 → **웃음 판별(진짜 vs 억지, Duchenne)** 또는 **질문 챌린지**
- 가장 반응이 터지는 구간
- 선 긋기: **"거짓말 탐지가 아니라 재미용 추정"**

### ④ 기술 설명 — 3분 (CS 청중이 듣고 싶은 부분)
데모로 흥미를 끈 다음 깊이를 보여줘야 학술제다워집니다. 아래 4개만 골라 깊게:

| 주제 | 핵심 |
|------|------|
| **블렌드셰이프 → 감정** | MediaPipe 얼굴 근육 52종 → 감정 7종 가중 조합 (예: 행복 = 미소×1.2 + 볼올림×0.4) |
| **rPPG 심박 추정** | POS(Wang et al. 2017) — 이마·볼 피부색 미세 변화로 심박 추정. "카메라로 맥박을 본다" |
| **Duchenne 미소** | 눈가 근육 동반 여부로 진짜/억지 웃음 판별 (심리학 근거) |
| **자동 보정** | 분포 기반 z-점수 보정(Welford 통계) — 측정할수록 정확해지는 구조 |

### ⑤ 소프트웨어 엔지니어링 한 스푼 — 1분
"모델만"이 아니라 **엔지니어링**을 보여주면 학과 정체성에 맞게 차별화됩니다:
- 추론을 **Web Worker로 분리**해 UI 멈춤 제거
- MediaPipe **lazy-load**, 정적 자산 1년 불변 캐싱
- **실제 청소년 행사 부스에서 돌아가는 배포된 서비스** (Vercel 라이브 URL)

### ⑥ 마무리 — 30초
- 운영 주체(성빈센트청소년회 × 한국사회공헌협회 청년챔프단 Butterfly) 언급
- *"기술이 재미와 공익에 닿는 사례"*
- 라이브 URL/QR 띄우고 **"지금 폰으로 해보세요"** → 깔끔한 마무리

---

## 📐 마음 들여다보기 — 수치 기준점 (질문 대비용)

> CS 청중은 반드시 *"그 숫자 근거가 뭐냐"*를 묻습니다. 아래는 **실제 코드의 공식·임계값** 그대로입니다.
> (전부 0~100 스케일, 의료 진단 아님 · 근거 있는 추정)

### 1) 감정 7종 점수 — 블렌드셰이프 가중 조합
MediaPipe FaceLandmarker가 내보내는 **얼굴 근육 52종(ARKit 스타일, 0~1)** 을 가중합한 뒤 ×100:

| 감정 | 공식 (블렌드셰이프 → ×100) |
|------|------|
| 행복 happy | (mouthSmile×1.2 + cheekSquint×0.4) |
| 슬픔 sad | (mouthFrown×0.9 + browInnerUp×0.5) |
| 분노 angry | (browDown×1.0 + eyeSquint×0.4 + mouthPress×0.5) |
| 놀람 surprised | (eyeWide×0.7 + browOuterUp×0.6 + jawOpen×0.5) |
| 불쾌 disgusted | (noseSneer×1.2 + mouthUpperUp×0.6) |
| 불안 fearful | (eyeWide×0.4 + browInnerUp×0.4 + mouthStretch×0.7) |
| 평온 neutral | 100 − (위 6종 합) |

- 좌/우가 있는 항목(browDown 등)은 **좌·우 평균**을 사용
- 각 점수는 0~100으로 clamp

### 2) 스트레스 지수 (0~100)
10초 평균 감정 점수를 다음 식으로 종합:

```
스트레스 = (슬픔 + 분노 + 불쾌 + 불안×0.8) + 놀람×0.3 − 행복×0.5
         → 0~100으로 clamp
```

- 부정 감정은 그대로 더하고, **불안은 0.8**, **놀람은 0.3**(놀람은 부정·긍정 모두 가능해서 약하게)
- **행복은 0.5만큼 차감**(긍정 감정이 스트레스를 상쇄)

### 3) 3단계 판정 임계값
| 구간 | 판정 | 메시지 |
|------|------|--------|
| 0 ~ 32 | **낮음** | 상태 양호 |
| 33 ~ 65 | **보통** | 약간의 스트레스, 휴식 권장 |
| 66 ~ 100 | **높음** | 높은 스트레스, 전문가 도움 고려 |

### 4) 표정 묘사 강도 기준
- 주요 감정 점수 **≥60 → 강함**("환한 미소"), **≥30 → 보통**("잔잔한 미소"), **<30 → 옅음**("옅은 미소")
- 두 번째로 강한 감정(평온 제외)이 **≥15** 면 "~의 기색도 함께 보였어요"로 같이 언급

### 5) 자동 보정 — "측정할수록 정확해짐"의 정체
기기에 익명 통계(평균·분산, **Welford 알고리즘**)를 누적해 분포 기준으로 보정:

- **15회 미만**이면 보정하지 않고 원점수 그대로 (`MIN_SAMPLES = 15`)
- z-점수: `z = (원점수 − 평균) / 표준편차` (표준편차 최소 5로 과보정 방지)
- 상대 점수: `50 + z×20` 을 0~100으로 clamp
- 혼합 비중: `weight = min(0.6, 측정횟수/100)` → **최대 60%까지만** 분포 보정 반영
- 최종 = `원점수×(1−weight) + 상대점수×weight`
- ※ 영상·개인식별 정보는 저장 안 함, localStorage 통계만

### 6) 심박수 추정 (rPPG · POS, 참고용)
- 신호: 이마·볼 ROI의 **R/G/B 평균** 시계열 → POS(Wang 2017) 투영으로 펄스 추출
- 처리: 선형 추세 제거(detrend) → **Hann 윈도우** → 관심 대역 직접 DFT
- 탐색 대역: **0.7 ~ 4.0 Hz = 42 ~ 240 bpm**, 해상도 약 1.2 bpm(0.02Hz step)
- 최소 요건: **4초 이상 + 16샘플 이상 + 샘플레이트 2Hz 이상**
- BPM = (피크 주파수 × 60) 반올림
- 신뢰도 = 피크 파워가 대역 평균 대비 얼마나 우세한가(SNR 유사): `(SNR−3)/12` 를 0~1로 clamp

> **발표 멘트 예시**: *"행복은 미소×1.2에 볼 올라감×0.4를 더해 계산하고, 스트레스는 부정 감정 합에서 행복을 절반만큼 빼요. 그리고 33점·66점을 경계로 3단계로 나눕니다. 측정이 15번 넘게 쌓이면 이 기기 환경 분포에 맞춰 자동 보정까지 들어가요."*

---

## 🛠 구현 상세 — 어떻게 만들었나 (기술 시연용)

> 학과 청중에겐 *"어떤 기술로, 어떻게 짰는지"* 가 핵심. 아래는 **실제 아키텍처와 코드**입니다.
> 슬라이드에 데이터 흐름도 1장 + 코드 스니펫 2~3개면 충분히 임팩트 있습니다.

### 기술 스택 (정확한 구성)

| 영역 | 사용 기술 | 역할 |
|------|-----------|------|
| 프론트엔드 | **React 19 + TypeScript + Vite** | UI, 상태, 빌드 |
| 얼굴 인식 | **MediaPipe FaceLandmarker** (`@mediapipe/tasks-vision`, WASM) | 브라우저 내 추론, 랜드마크 468점 + 블렌드셰이프 52종 |
| 추론 격리 | **Web Worker + OffscreenCanvas** | 추론·드로잉을 메인 스레드에서 분리 |
| 심박 추정 | **POS rPPG** (Wang et al. 2017) + 직접 DFT | 피부색 미세 변화 → BPM |
| 통계 보정 | **Welford 알고리즘** + localStorage | 분포 기반 자동 보정 |
| 애니메이션 | **Lottie** (Noto Animated Emoji) | 벡터 이모지 |
| 배포 | **Vercel** (master push 자동 배포) | 정적 자산 1년 불변 캐시 |

### 전체 데이터 흐름 (한 장으로 설명)

```
웹캠 <video>
   │  매 100ms (10fps)
   ▼
createImageBitmap(video)  ──transfer──▶  [Web Worker]
                                          ├─ MediaPipe FaceLandmarker 추론
                                          ├─ 랜드마크 오버레이 드로잉 (OffscreenCanvas)
                                          └─ 이마·볼 ROI의 RGB 평균 샘플링
   ┌──────────────── { frame, sample } ◀──┘
   ▼
메인 스레드
   ├─ 블렌드셰이프 52종 → 감정 7종 점수 (가중합)
   ├─ RGB 샘플 누적 → POS rPPG → DFT → BPM
   ├─ 10초 평균 → 스트레스 지수 → 3단계 판정
   └─ Welford 분포 보정 → 결과 화면 (카운트업 연출)
```

**핵심 설계 포인트 (말로 강조할 것):**
- **영상이 서버로 안 나감** — 추론·계산 전부 브라우저 안에서. 서버는 정적 파일만 전달
- **메인 스레드를 비움** — 무거운 추론을 워커로 보내 UI(React·Lottie)가 안 끊김
- **자동 폴백** — Worker/OffscreenCanvas 미지원 브라우저는 메인 스레드 경로로 자동 전환 (공개 인터페이스 동일)

### 코드 ① 감정 점수 — 블렌드셰이프 가중 조합
```ts
// 얼굴 근육 52종(0~1) → 감정 7종(0~100)
const happy     = clamp((smile * 1.2 + cheekSquint * 0.4) * 100);
const angry     = clamp((browDown * 1.0 + eyeSquint * 0.4 + mouthPress * 0.5) * 100);
const surprised = clamp((eyeWide * 0.7 + browOuterUp * 0.6 + jawOpen * 0.5) * 100);
// ...
scores.neutral  = clamp(100 - (happy + sad + angry + surprised + disgusted + fearful));
```

### 코드 ② 추론을 워커로 — 메인 스레드 비우기
```ts
// 메인: 매 프레임 비디오를 비트맵으로 떠서 워커로 transfer (복사 아님, 소유권 이전)
const bitmap = await createImageBitmap(video);
worker.postMessage({ type: 'detect', id, bitmap, w, h }, [bitmap]);

// 워커: 추론 + 오버레이 드로잉 + ROI 샘플링 후 결과만 반환
const result = landmarker.detectForVideo(bitmap, nextTs());
const out = buildFrame({ result, drawCtx: overlayCtx, sampleCtx, source: bitmap, ... });
postMessage({ type: 'detect-result', id, frame: out.frame, sample: out.sample });
```

### 코드 ③ rPPG — 카메라로 맥박 보기
```ts
// 이마·볼 ROI의 R/G/B 평균 시계열을 POS 투영 → 펄스 신호
s1[i] = gn - bn;             // [ 0, 1, -1]
s2[i] = -2 * rn + gn + bn;   // [-2, 1,  1]
const alpha = std(s1) / std(s2);     // 두 투영 결합
H[m + i] += (s1[i] + alpha * s2[i]); // overlap-add 누적
// → detrend → Hann 윈도우 → 0.7~4.0Hz 대역 DFT → 피크 주파수 × 60 = BPM
```

### 엔지니어링으로 차별화할 포인트 (SW공학과니까)
- **Web Worker로 추론 격리** → UI 프레임 드랍 제거
- **MediaPipe 동적 import** → 메인 번들에서 분리, 측정 시작할 때만 로드
- **모델/WASM 1년 불변 캐싱** → 재방문 즉시 시작
- **GPU delegate 우선, 실패 시 CPU 폴백** → 기기 호환성
- **단조 증가 타임스탬프 관리** (MediaPipe VIDEO 모드 요구사항) 등 디테일

---

## 라이브 데모 리스크 대비 (데모의 생명줄)

1. **조명** — 표정·심박 정확도가 조명에 크게 좌우됨. 발표장 조명 미리 확인, 어두우면 보조 조명 준비
2. **카메라 권한 + HTTPS** — 라이브 URL(https)로 미리 권한 허용해두기 (localhost도 가능)
3. **백업 영상** — 데모가 깨질 때 대비해 **미리 녹화한 화면 영상** 준비. 이거 하나로 발표가 안 망함
4. **네트워크** — 첫 로드 시 MediaPipe wasm/모델(수 MB) 다운로드. **발표 직전 미리 열어 캐싱**. 현장 와이파이 믿지 말 것
5. **객석 폰 QR** — 동시 다수 접속해도 각자 브라우저에서 돌아 서버 부하 0 → "확장성"도 어필 포인트

---

## 예상 Q&A

| 질문 | 답변 |
|------|------|
| **정확한가요?** | 감정·심박은 재미·참고용 추정, 의료 진단 아님. 다만 블렌드셰이프·POS·Duchenne 등 **근거 있는 신호** 사용 |
| **프라이버시는?** | 영상 저장·전송 0. 전부 클라이언트(브라우저) 추론 |
| **왜 서버가 아니라 브라우저?** | 프라이버시 + 무한 동시접속 + 인프라 비용 0 |

---

## 발표 전 체크리스트

- [ ] 노트북 + 웹캠 동작 확인
- [ ] 라이브 URL 미리 열어 모델 캐싱 + 카메라 권한 허용
- [ ] 발표장 조명 확인 (필요 시 보조 조명)
- [ ] 백업 데모 녹화 영상 준비
- [ ] 객석 참여용 QR 코드 준비
- [ ] 자원자 데모 시나리오(웃음 판별/질문 챌린지) 미리 정하기
